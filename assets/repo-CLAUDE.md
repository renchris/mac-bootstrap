# <project name>

<One sentence: what this repo is and who runs it.>

## Commands — the only ones; never guess a script or a package manager
- Install `<cmd>` · Dev `<cmd>` · Build `<cmd>`
- Test `<cmd>` — single test: `<cmd>`
- Lint + typecheck `<cmd>` — run both before every commit

## Layout
- `<dir>/` — <what lives there>
- `<dir>/` — <what lives there>

## Rules an agent would get wrong unless told
- <rule>
- <rule>

## Never
- <the destructive or irreversible thing here, and what to do instead>
